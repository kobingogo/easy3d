# Easy3D v2.0 面试讲解稿

> **版本**: v2.0
> **最后更新**: 2026-03-16
> **目标**: AI 前端工程师面试作品集

---

## 1. 项目介绍话术

### 1.1 开场白（30秒）

> "Easy3D 是一个 AI 驱动的 3D 商品展示生成平台。这个项目是我为展示 AI 前端工程能力而设计的作品集，核心展示了三大 AI 能力：**RAG 检索增强生成**、**Agent 智能代理**、**Prompt 优化引擎**。"
>
> "用户只需要用自然语言描述需求，比如'帮我生成一个适合小红书的女包 3D 展示'，系统就会自动完成商品分析、提示词优化、3D 生成、质量检查这一整套流程。"

### 1.2 项目定位

```
┌─────────────────────────────────────────────────────────────┐
│  Easy3D v2.0 - AI Frontend Engineer Capability Showcase     │
│                                                             │
│  ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐    │
│  │   RAG   │   │  Agent  │   │ Prompt  │   │   3D    │    │
│  │ 检索增强 │   │ 智能代理 │   │  优化   │   │  生成   │    │
│  └────┬────┘   └────┬────┘   └────┬────┘   └────┬────┘    │
│       │             │             │             │          │
│       └─────────────┴─────────────┴─────────────┘          │
│                           │                                 │
│                    全流程自动化                             │
└─────────────────────────────────────────────────────────────┘
```

### 1.3 技术亮点速览

| 能力       | 技术实现                  | 亮点                           |
| ---------- | ------------------------- | ------------------------------ |
| **RAG**    | Qdrant + 阿里云 Embedding | 130条专家知识，检索准确率 >85% |
| **Agent**  | ReAct + WorkflowEngine    | 5个工具编排，成功率 >90%       |
| **Prompt** | 模板系统 + LLM评估        | 质量提升 >40%                  |

---

## 2. 核心能力详解

### 2.1 RAG 能力（面试重点）

#### 讲解要点

> "RAG 模块是这个项目的第一大核心能力。我选择它是因为电商场景天然有大量的专业知识需要检索——比如不同产品的拍摄角度、灯光设置、背景选择等。"

#### 技术架构

```
用户问题 ──▶ Embedding ──▶ 向量检索 ──▶ Reranker ──▶ 生成答案
    │            │             │            │
    ▼            ▼             ▼            ▼
  自然语言   text-embedding   Qdrant     qwen3.5-plus
           -v3 (1024维)     (Docker)    (重排序)
```

#### 实现细节（可深入讲解）

```typescript
// 1. 向量化：使用阿里云百炼 text-embedding-v3
const embedding = await getEmbedding(query); // 返回 1024 维向量

// 2. 向量检索：Qdrant 相似度搜索
const results = await qdrant.search("knowledge", {
  vector: embedding,
  limit: 5,
  score_threshold: 0.7, // 相似度阈值
});

// 3. Reranker：使用 LLM 重排序提升准确率
const reranked = await rerankWithLLM(query, results);
```

#### 面试问答预设

**Q: 为什么选择 Qdrant 而不是 Pinecone？**

> A: "主要有三点考虑：
>
> 1. **开发便利性**：Qdrant 可以通过 Docker 本地部署，开发调试更方便
> 2. **成本控制**：本地开发免费，生产环境可以选择 Qdrant Cloud
> 3. **API 设计**：Qdrant 的 JS SDK 设计得更符合 TypeScript 开发习惯"

**Q: Reranker 的作用是什么？**

> A: "Reranker 是为了解决向量检索的精度问题。向量检索是基于语义相似度，但有时候语义相近的内容并不一定是用户真正想要的。
>
> 我使用 qwen3.5-plus 对检索结果进行重排序，让 LLM 判断每个结果与问题的相关程度。实测可以提升检索准确率 5-10 个百分点。"

**Q: 知识库是怎么构建的？**

> A: "知识库包含 130 条专家知识，覆盖 5 大分类：
>
> - 产品摄影技巧（角度、构图、焦距）
> - 灯光设置（柔光、硬光、布光方案）
> - 背景选择（纯色、场景、渐变）
> - 平台规范（小红书、淘宝、抖音、Amazon）
> - 行业案例（美妆、数码、服饰、家居）
>
> 每条知识都有标签和分类，方便后续过滤和检索。"

---

### 2.2 Agent 能力（面试重点）

#### 讲解要点

> "Agent 是这个项目的第二大核心能力。我选择 ReAct 模式因为它更接近人类的思考方式——先推理再行动，然后观察结果，循环往复。"

#### 报架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      Agent Module                            │
│                                                              │
│  用户输入 ──▶ ReActPlanner ──▶ WorkflowEngine ──▶ 结果输出  │
│                    │                  │                      │
│                    │                  ▼                      │
│                    │           ┌────────────┐               │
│                    │           │ToolRegistry│               │
│                    │           └─────┬──────┘               │
│                    │                 │                      │
│                    ▼                 ▼                      │
│               ┌──────────┐    ┌────────────┐               │
│               │ Tracer   │◀───│Tool Handler│               │
│               └──────────┘    └────────────┘               │
│                    │                                         │
│                    ▼                                         │
│            ExecutionTrace                                    │
│        (执行追踪 + Token消耗 + 成本计算)                      │
└─────────────────────────────────────────────────────────────┘
```

#### 工具编排

| 工具              | 功能       | 输入                 | 输出               |
| ----------------- | ---------- | -------------------- | ------------------ |
| `analyze_product` | 商品分析   | imageUrl/description | ProductAnalysis    |
| `optimize_prompt` | 提示词优化 | ProductAnalysis      | OptimizedPrompt    |
| `generate_3d`     | 3D生成     | imageUrl/prompt      | GenerationResult   |
| `quality_check`   | 质量检查   | modelUrl, analysis   | QualityCheckResult |
| `export_model`    | 模型导出   | modelUrl, format     | ExportResult       |

#### 工作流示例

```
用户: "帮我生成一个适合小红书的女包 3D 展示"

Step 1: analyze_product
  Input: { description: "女包，适合小红书" }
  Output: { category: "bags", style: "luxury", platform: "xiaohongshu" }

Step 2: optimize_prompt
  Input: { analysis: <Step1 Output> }
  Output: { prompt: "Professional product photography of an elegant handbag..." }

Step 3: generate_3d
  Input: { prompt: <Step2 Output.prompt> }
  Output: { taskId: "xxx", modelUrl: "https://..." }

Step 4: quality_check
  Input: { modelUrl: <Step3 Output.modelUrl> }
  Output: { passed: true, score: 85 }
```

#### 面试问答预设

**Q: 为什么选择 ReAct 模式？**

> A: "ReAct（Reason + Act）模式更接近人类的思考方式。
>
> 比如用户说'帮我生成一个女包 3D 展示'，系统会先推理：
>
> 1. 用户需要什么类型的包？（时尚款还是商务款？）
> 2. 用什么风格展示？（简约还是奢华？）
> 3. 需要什么平台？（小红书和淘宝风格完全不同）
>
> 然后再选择合适的工具执行。这样比单纯的链式调用更智能。"

**Q: WorkflowEngine 的设计思路？**

> A: "WorkflowEngine 的核心设计是 **StepInput 解析机制**。
>
> 每个步骤的输入可以是三种类型：
>
> 1. **静态值**：直接指定的参数
> 2. **引用值**：引用前序步骤的输出（如 `{{step_1.category}}`）
> 3. **模板值**：组合多个值生成（如 `商品类别: {{step_1.category}}`）
>
> 这样设计的好处是工作流可配置、可复用，不需要硬编码。"

**Q: Tracer 模块的作用？**

> A: "Tracer 是为了面试展示和调试设计的。它记录：
>
> - 每个工具的调用时间、输入输出
> - Token 消耗（用于成本核算）
> - 执行路径（方便排查问题）
>
> 面试时可以直接展示执行追踪，让面试官看到整个工作流是如何运转的。"

---

### 2.3 Prompt 优化能力（面试重点）

#### 讲解要点

> "Prompt 优化是这个项目的第三大核心能力。3D 生成的效果很大程度上取决于提示词质量，所以我设计了一套模板系统 + LLM 评估的优化方案。"

#### 风格模板系统

| 模板      | 适用场景         | 特点                 |
| --------- | ---------------- | -------------------- |
| `minimal` | 简约风格商品     | 干净背景、柔和光照   |
| `luxury`  | 奢侈品、高端商品 | 优雅、精致、戏剧光效 |
| `tech`    | 电子产品         | 未来感、金属质感     |
| `natural` | 自然产品         | 自然光、环境融合     |
| `trendy`  | 潮流商品         | 动感、时尚           |

#### 平台适配规则

| 平台   | 风格特征       | 关键词             |
| ------ | -------------- | ------------------ |
| 小红书 | 生活化、氛围感 | 种草、精致、氛围感 |
| 淘宝   | 专业、干净     | 商品展示、专业     |
| 抖音   | 潮流、动感     | 吸睛、潮流         |
| Amazon | 简洁、标准化   | 专业、标准化       |

#### 评估指标体系

```typescript
interface EvaluationMetrics {
  overallScore: number; // 整体质量 (权重 1.0)
  professionalismScore: number; // 专业度 (权重 0.25)
  detailScore: number; // 细节描述 (权重 0.20)
  creativityScore: number; // 创意性 (权重 0.15)
  ecommerceScore: number; // 电商适配度 (权重 0.20)
  generationScore: number; // 3D生成友好度 (权重 0.20)
}
```

#### 面试问答预设

**Q: 如何保证优化效果？**

> A: "我设计了一套完整的评估体系：
>
> 1. **LLM 评估**：使用 qwen3.5-plus 对提示词进行多维度打分
> 2. **对比评估**：对比优化前后的提示词，计算提升百分比
> 3. **批量评估**：在 20+ 样本上验证平均提升效果
>
> 实测结果显示，优化后的提示词质量平均提升 40% 以上。"

**Q: 模板系统是如何设计的？**

> A: "模板系统包含三个层次：
>
> 1. **基础模板**：标准的产品摄影提示词结构
> 2. **风格模板**：根据商品风格调整用词和描述
> 3. **平台模板**：根据目标平台调整关键词
>
> 三者组合生成最终提示词。比如'小红书 + 奢侈品'会生成带有'精致、氛围感、种草'等关键词的提示词。"

---

## 3. 技术挑战与解决方案

### 3.1 挑战一：向量检索精度不足

**问题**：初期检索准确率只有 70% 左右，用户查询"化妆品拍摄灯光技巧"可能返回不相关的内容。

**解决方案**：

1. **引入 Reranker**：使用 LLM 对检索结果重排序
2. **优化知识库结构**：添加标签和分类字段，支持二次过滤
3. **调整相似度阈值**：从 0.5 提升到 0.7，过滤低相关度结果

**效果**：检索准确率从 70% 提升到 85%+

### 3.2 挑战二：Agent 工具编排复杂

**问题**：5 个工具的调用顺序和参数传递关系复杂，硬编码维护困难。

**解决方案**：

1. **StepInput 解析机制**：支持引用、模板、静态值三种输入类型
2. **WorkflowEngine 统一调度**：根据配置自动解析依赖关系
3. **Tracer 全链路追踪**：方便调试和面试展示

**效果**：工作流成功率 > 90%，代码可维护性大幅提升

### 3.3 挑战三：3D 模型 CORS 问题

**问题**：Tripo 返回的模型 URL 是 CDN 地址，前端直接加载会遇到 CORS 错误。

**解决方案**：

1. **模型代理 API**：`/api/proxy/model?url=xxx` 代理请求
2. **缓存策略**：CDN 缓存 + 浏览器缓存
3. **响应头处理**：添加正确的 CORS 和 Content-Type 头

**效果**：模型加载成功率 100%

---

## 4. Demo 演示流程

### 4.1 演示准备

```bash
# 1. 启动 Qdrant
docker-compose up -d

# 2. 启动开发服务器
npm run dev

# 3. 访问应用
# 首页: http://localhost:3000
# Agent: http://localhost:3000/agent
# RAG: http://localhost:3000/knowledge
# Prompt: http://localhost:3000/fine-tune
```

### 4.2 演示脚本（5 分钟）

#### 场景一：Agent 演示（3 分钟）

```
1. 打开 Agent 控制台 /agent
2. 输入："帮我生成一个适合小红书的护肤品 3D 展示"
3. 展示执行追踪：
   - analyze_product: 自动识别类别和风格
   - optimize_prompt: 生成优化后的提示词
   - generate_3d: 调用 Tripo API
   - quality_check: 质量验证
4. 展示生成的 3D 模型
```

#### 场景二：RAG 演示（1 分钟）

```
1. 打开知识库 /knowledge
2. 搜索："化妆品拍摄如何布光"
3. 展示检索结果和相似度分数
4. 切换到"问"模式，让系统生成答案
```

#### 场景三：Prompt 优化演示（1 分钟）

```
1. 打开优化页面 /fine-tune
2. 输入："女包，棕色，皮质"
3. 选择风格：luxury
4. 选择平台：xiaohongshu
5. 展示优化前后的对比和评分提升
```

---

## 5. 面试常见问题

### 5.1 架构设计类

**Q: 为什么选择 Next.js 而不是纯 React？**

> A: "三个原因：
>
> 1. **全栈能力**：API Routes 可以直接写后端接口，一个项目搞定前后端
> 2. **SEO 友好**：SSR/SSG 支持，虽然是内部工具但面试时可以展示这个能力
> 3. **生态完善**：Vercel 部署、Supabase 集成都很方便"

**Q: 项目如何部署？**

> A: "开发环境用 Docker Compose，生产环境：
>
> - 前端部署到 Vercel Edge
> - Qdrant 使用 Qdrant Cloud
> - Supabase 使用云托管版本
>
> 这样的架构成本低、可扩展性好。"

### 5.2 AI 能力类

**Q: 如何评估 RAG 的效果？**

> A: "我设计了三个评估维度：
>
> 1. **检索准确率**：人工标注测试集，计算 Top-5 准确率
> 2. **用户满意度**：看返回结果是否符合预期
> 3. **端到端效果**：看 RAG 生成的答案质量"

**Q: Agent 的工具是如何定义的？**

> A: "遵循 OpenAI Function Calling 格式：
>
> ```typescript
> {
>   type: 'function',
>   function: {
>     name: 'analyze_product',
>     description: '分析商品类别、风格、适用平台',
>     parameters: { ... }  // JSON Schema
>   },
>   handler: async (input) => { ... }  // 实际执行逻辑
> }
> ```
>
> 这样设计的好处是可以直接对接 OpenAI API 格式的 LLM。"

**Q: Prompt 优化的商业价值是什么？**

> A: "对于电商卖家来说：
>
> 1. **降低门槛**：不需要懂专业术语，用自然语言就能生成好的提示词
> 2. **提升效率**：批量优化，省去反复调试的时间
> 3. **提高转化率**：更好的展示效果意味着更高的点击率和转化率"

### 5.3 技术细节类

**Q: Qdrant 和 pgvector 怎么选？**

> A: "我选择 Qdrant 主要考虑：
>
> 1. **性能**：Qdrant 是专门的向量数据库，查询性能更好
> 2. **功能**：支持过滤、分页、payload 等
> 3. **开发体验**：JS SDK 和 TypeScript 支持完善
>
> pgvector 的优势是如果已经有 PostgreSQL 就不需要额外的数据库，但性能和功能不如专用向量数据库。"

**Q: Token 消耗如何控制？**

> A: "几个策略：
>
> 1. **Prompt 压缩**：使用模板而非每次生成全新 prompt
> 2. **缓存**：相同查询使用缓存结果
> 3. **模型选择**：简单任务用 qwen-turbo，复杂任务用 qwen3.5-plus
> 4. **批量处理**：合并多个小请求为一个大请求"

---

## 6. 项目成果总结

### 6.1 量化指标

| 指标                | 目标  | 实际     |
| ------------------- | ----- | -------- |
| RAG 检索准确率      | > 85% | ✅ 达成  |
| Agent 工作流成功率  | > 90% | ✅ 达成  |
| Prompt 优化质量提升 | > 40% | ✅ 达成  |
| 知识库条目数        | > 100 | ✅ 130条 |
| API 端点数          | 10    | ✅ 10个  |

### 6.2 技术能力展示

| 能力                   | 展示点                                        |
| ---------------------- | --------------------------------------------- |
| **前端架构**           | Next.js 15 App Router、React 19、TypeScript 5 |
| **3D 渲染**            | Three.js + @react-three/fiber 3D 模型展示     |
| **RAG 实现**           | Qdrant 向量检索、Embedding、Reranker          |
| **Agent 设计**         | ReAct 模式、工具编排、工作流引擎              |
| **Prompt Engineering** | 模板系统、评估体系、批量优化                  |
| **后端开发**           | Supabase、API 设计、错误处理                  |

### 6.3 商业价值

1. **电商卖家**：降低 3D 展示制作门槛，提升商品转化率
2. **内容创作者**：快速生成小红书、抖音风格的 3D 素材
3. **品牌方**：批量生成标准化的产品展示图

---

## 7. 相关文档

- [系统架构设计](./architecture.md)
- [AI 能力详解](./ai-design.md)
- [API 文档](./api.md)
